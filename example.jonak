FUN msg_contain(hello_msg, world_msg)
	VAR real_msg = hello_msg + ", " + world_msg + "!"
	RETURN real_msg
END

VAR hello = "Hello"
VAR world = "World"
VAR msg = msg_contain(hello, world)

FOR i = 0 TO 5 THEN
	PRINT(msg)
END
